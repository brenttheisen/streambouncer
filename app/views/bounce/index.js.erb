var html = "<%= escape_javascript(render(:partial => 'search/bounce_pop_up', :locals => { :follow => @follow })) %>";
$('#follow_<%= @follow.id %>')
  .removeClass('no-bounce')
  .addClass('bounce')
  .prepend(html);
$('#bounce-pop-out').hide();
